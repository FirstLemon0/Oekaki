# 成長通（せいちょうつう） UI/デザイン ブリーフ v1

Claude Design に渡す用。ここに書いた仕様は DESIGN.md（構想書）から UI に必要な部分だけを抜き出し、画面ごとに具体化したもの。
成果物は §9 の形式で返してもらい、Preact＋CSS 変数で実装する。

---

## 1. プロダクト概要

- **一言**: 画力ゼロの人がタブレット＋ペンで、アニメ・漫画調のキャラクターイラスト1枚絵を描けるようになるまで伴走する「イラスト版 Duolingo」。
- **ユーザー**: 本人1人（大人・完全な初心者）。他人に見せる機能はない。
- **使い方**: 平日15分・休日30分。毎日1レッスン。12〜18ヶ月の長期利用。
- **名前**: 成長通（せいちょうつう）。「成長への通り道（一本道）」と「成長に通じている」の二重の意味。ロゴ／アイコンも欲しい。
- **体験の核**:
  1. 一本道のパスを1日1歩進む（次にやることは常に1つ）。
  2. 描いた線がその場で採点される（線の精度は点数、絵全体は AI の言葉で批評、と使い分ける）。
  3. 「うまく描けたか」より「やったか」を褒める。ストリークと累計カウンターが伸びる。
  4. Before/After で自分の伸びを目で見る。

## 2. 端末・入力条件（最重要）

- **端末**: Lenovo Yoga Tab Plus（Android、12.7インチ、約2944×1840、DPR 約2 → CSS で約 **1472×920**）。Chrome の PWA として全画面で動く。
- **向き**: **横持ちが主**（キックスタンドで立てて描く）。縦持ちも崩れない程度に対応（1枚だけ縦のモックが欲しい）。
- **入力**: ペン（筆圧・傾きあり）とタッチの両方。描画中はペン専用、UI 操作はどちらでも。
- **利き手**: 右利き想定。キャンバス画面のツールバーは**左端**または**上端**（右手で描くときに手で隠れない位置）。左利き切替を設定に置く。
- **タップ目標**: 指で押すものは 48px 以上、ペン専用の小さなボタンでも 36px 以上。
- **キャンバス画面の鉄則**: 描いている最中に動くもの・光るもの・通知を出さない。UI はキャンバスの端に寄せ、半透明。ペンの遅延を生む重い効果（ぼかし・影の多用）を描画面に重ねない。
- **文字サイズ**: 本文 16〜18px。説明図の横に置く短文が多い。日本語フォント（Noto Sans JP 系）。

## 3. 情報設計と画面遷移

```
[ホーム(パス)] ─┬─ [レッスン] ─┬─ [キャンバス] ─ [採点結果パネル] ─ 次のステップ/完了モーダル
                │              ├─ [説明(read)] / [クイズ]
                │              └─ [批評(B)] ─ 送信確認 → 待機 → 結果
                ├─ [自由お絵描き] → [キャンバス]（採点なし）
                ├─ [ギャラリー] ─ [絵の詳細]（ストローク再生・批評履歴）
                └─ [設定] ─ [校正モード] / [バックアップ]
```

下部または左のナビは **ホーム／ギャラリー／設定** の3つだけ。自由お絵描きはホームの目立つ位置に常設ボタン。

## 4. 画面ごとの仕様

### 4.1 ホーム（パス）

Duolingo 2022 以降の「一本道パス」型。スキルツリーの自由選択ではない。

要素:
- 上部バー: ストリーク（炎アイコン＋日数）、フリーズ保有数（最大2）、今日の目標の達成状態、XP/レベル（控えめ）。
- パス本体: 縦にうねる一本道。ノード＝レッスン。状態は **完了／今日やる（光る・1つだけ）／ロック**。
  - ステージ見出しのバナー（例「ステージ2 形と立体」＋進捗 12/28）。
  - ユニットの区切り（小さな見出し）。
  - ステージ末に「模写チェックポイント」ノードと「卒業課題」の門（大きめ・別形状）。
  - 復習カードが差し込まれた日は、今日のノードの手前に「復習」ノードが1つ現れる。
- 累計カウンター帯: 直線 1,234本／楕円 512個／円 300個／箱 87/250／ジェスチャー 40回／完成した絵 3枚。横スクロール可。
- 常設ボタン: 「自由お絵描き」（採点なし・記録だけ、と小さく添える）。
- 通知カード（必要時のみ）: 「今月の Before/After を描き直しませんか」「バックアップから30日経過」。
- 状態: 初回起動（Before を描くよう促す）／通常／今日完了済み（次のノードは翌日、追加ドリル・自由枠を提案）／ストリーク危機（今日まだ・22時以降）。

### 4.2 レッスン画面（ステップ進行）

1レッスン＝5〜9ステップ。上部に進捗バー（ステップ数）。1ステップ＝1画面。

ステップ型と画面:
- **read（説明）**: 左に SVG 図解、右に短文（3〜5行）。「次へ」。図解はアタリ線・矢印・番号つきの構築図が中心で、「上手い絵」は出ない。
- **drill（ドリル）**: 課題文（例「水平線を10本。ゴースティングしてから一気に引く」）→「描く」でキャンバスへ。目標本数と現在本数を表示。
- **trace（なぞり）**: お手本が薄く表示されたキャンバスへ。
- **copy（横に見て描く）**: 左にお手本・右にキャンバス（分割）。描き終えたら重ねて比較。
- **construct（構築手順）**: 手順（1 アタリ → 2 形 → 3 細部）をステップ表示しながら同じキャンバスに描き足す。左上に現在の手順と小さな図解。
- **gesture（ジェスチャー）**: 左にポーズ人形（3D、回転可）、右にキャンバス、上に残り秒数の大きな数字（30/60/90）。時間切れで自動終了 → 見比べ画面（自分の絵とポーズを並べる／重ねる）。
- **quiz（クイズ）**: 選択式（画像2〜4択）。正誤と1行解説。
- **mosha（模写チェックポイント）**: 取込画像を横に見て模写 → 差分を自分でタップして印を付ける画面（お手本と自分の絵を並べ、ズレた箇所にマークを置く）→「1か所だけ変えて描き直す」。
- **critique（卒業課題）**: 描く（アプリ内）または画像を取り込む → 批評画面へ。
- **submit（外部アプリ提出）**: 「外部アプリで描いてから戻ってきてください」の案内 → 画像取込（共有シート／ファイル選択）→ 批評画面へ。

レッスン完了モーダル: 獲得 XP、伸びたカウンター、ストリーク更新、「明日はこれ」（次ノード名）。派手すぎない祝福。連続で押せる「もう1本（追加ドリル）」「自由お絵描き」「ホームへ」。

### 4.3 キャンバス画面（最重要画面）

- 全画面キャンバス。紙色は純白ではなく薄いウォームグレー（長時間見ても疲れない）。
- **ツールバー**（左端縦 or 上端横、半透明、折り畳み可）:
  ペン／消しゴム／Undo／Redo／全消し／お手本の不透明度スライダー（0〜100%）／グリッド（なし・3分割・4分割）／左右反転／シルエット化／描画再生／ヘルプ（課題文を再表示）。
- **課題インストラクション**: 上部に1行（例「楕円を20個。度合いを揃える」）。折り畳むと小さなピルになる。
- **カウンター**: ドリル中は「12/20」のような進捗を右上に小さく。
- **タイマー**: gesture のときだけ大きく。
- **完了ボタン**: 右下（右手側）。誤タップ防止に少し内側。
- **ペン専用モードの表示**: 「ペンのみ」インジケータ（タッチで描けない理由が分かるように）。
- 状態: 描画中／お手本表示中／再生中／採点待ち（一瞬）／採点結果パネル表示。

### 4.4 採点結果パネル（A）

キャンバス上に下からせり上がるシート（キャンバスの絵は見えたまま）。
- 大きな点数（0〜100）と、自己ベストとの差（▲+4）。
- **ヒートマップ**: 自分の線を、ズレの大きさで色分けして表示（緑→黄→赤）。目標線（理想）を薄く重ねる。
- **1行の助言**（ルールベース、例「終点で速度が落ちて曲がっています。最後まで一定速度で」）。
- サブ指標: ブレ、直進度、到達、といった2〜4個の小さなバー。
- ボタン: 「もう一回」「次へ」。「もう一回」が主。
- 点数の意味の注記（小さく）: 「点数は線の精度だけを見ています」。

### 4.5 批評画面（B）

- **送信前確認**: 縮小プレビュー、「今日の残り 2/3 回」、送信する内容（自分の絵・課題文・ルーブリック）を明示、「お手本は送りません」。「送る」「やめる」。
- **待機**: 10〜30秒。静かなアニメーション＋「先生が見ています…」。キャンセル可。
- **結果**: 4ブロックのカード。
  - 良い点（2〜3、緑系）
  - 直す点（最大3。各「どこ／何が／どう直す」。自分の絵のサムネイルに番号マーカーを置く）
  - 次にやる1つ（強調・1つだけ）
  - 一言（励まし）
  - **数値スコアは出ない**。「点数はつけません。言葉で見ます」の注記。
- 履歴: 過去の批評を絵と並べて閲覧。
- エラー状態: キー未設定（設定へ誘導）／上限到達（明日また）／通信失敗（再送）／モデル未提供（自動フォールバックした旨）。

### 4.6 ギャラリー

- グリッド表示。フィルタ: すべて／ドリル／レッスン／自由／卒業課題／Before・After。
- **Before/After 比較**: 左右に並べる専用ビュー。月ごとの描き直しが時系列で並ぶ。
- 絵の詳細: 拡大、ストローク再生（描いた順に線が現れる）、採点結果、批評、削除、共有（端末内保存）。

### 4.7 設定

グループ分け:
- **AI 批評**: API キー（マスク表示・接続テスト）、モデル ID（既定 claude-opus-5-5）、思考の深さ（low/medium/high）、1日の上限回数、費用の目安表示。
- **採点**: 校正モード（直線・円・楕円を数本描いて基準を作る。結果と再実行）、合格ラインの厳しさ（ふつう／やさしめ）。
- **練習**: 1日の目標、通知時刻、利き手、ペン専用モード、外部お絵描きアプリ名。
- **データ**: ストレージ使用量、バックアップ書き出し（zip）、読み込み、最終バックアップ日。
- **外観**: ライト／ダーク／端末に合わせる、文字サイズ。
- **このアプリについて**: 名前・版・「点数の考え方」の説明ページ。

### 4.8 モーダル・通知

- レッスン完了、ステージ修了（大きめ・卒業課題の絵を表示）、ストリーク危機（22時以降・1回だけ）、フリーズ使用の確認、月次 Before/After、バックアップ推奨、ストレージ警告。

## 5. コンポーネント一覧

ナビゲーションバー／上部ステータス（ストリーク・フリーズ・目標）／パスノード（3状態＋門＋復習）／ステージバナー／カウンターチップ／ボタン（主・副・危険・アイコン）／ツールバー（縦・横）／スライダー／トグル／セグメント（グリッド選択など）／進捗バー／タイマー表示／点数表示／ヒートマップ凡例／サブ指標バー／カード（良い点・直す点・次の1つ）／番号マーカー／画像タイル／比較ビュー（並べる・重ねる）／ボトムシート／モーダル／トースト／空状態（ギャラリー空・キー未設定）／入力（テキスト・マスク付き）／リスト行（設定）。

## 6. ビジュアルの方向性

- **コンセプト**: 「紙とペンの落ち着き」＋「一本道を進む手応え」。Duolingo の原色の賑やかさは**踏襲しない**。絵の邪魔をしない低彩度の UI に、達成の瞬間だけ彩度の高いアクセントが差す。
- **色**: ベースは紙色（ウォームグレー〜生成り）、文字は墨色。アクセントは1色（成長を連想する色。候補: 若葉、朱、藍のいずれか。提案が欲しい）。採点の緑・黄・赤はアクセントと衝突しない色に。ダークモードは「黒い紙」ではなく「夜の机」程度の暗さ。
- **タイポグラフィ**: 日本語主体。見出しと本文で1書体（Noto Sans JP または Zen Kaku Gothic New 等）、数字（点数・タイマー・カウンター）は等幅寄りで大きく。
- **形**: 角丸は中程度。線画風のアイコン（太さ一定のストローク）。
- **モーション**: パスの進行・完了時だけ短く。キャンバス画面では無し。
- **ロゴ／アイコン**: 「成長通」の字と、一本道または一筆の線をモチーフに。ホーム画面アイコンは 512px 正方形、文字なし版も。

## 7. 文言のトーン

- 丁寧だが短い（1文20字前後）。命令形ではなく「〜しましょう」「〜でOK」。
- 完了を褒める。「うまい／へた」の評価語は使わない。
- 点数の意味を正直に書く（「線の精度」だけ）。
- 例: 「今日の分は終わり。明日は楕円の度合いです」「終点で少し速度が落ちました。最後まで一定で」「点数はつけません。言葉で見ます」。

## 8. やらないこと（NG）

- リーグ・ランキング・ハート（ライフ）制・課金演出。
- 点数だけで煽る演出、赤い大きな✕。
- キャンバス上のポップアップ・動く広告的要素。
- 他人の絵・キャラクターの画像を UI に載せること（お手本は構築線の図解か、ユーザー取込画像のみ）。
- 小さすぎるタップ目標、細い文字。

## 9. 欲しい成果物（この順で）

1. **デザイントークン**: 色（ライト／ダーク）、タイポスケール、スペーシング、角丸、影、状態色。**CSS 変数名つき**（例 `--color-accent`, `--color-paper`, `--score-good`）。
2. **コンポーネント仕様**: §5 の各コンポーネントの見た目と状態（通常／押下／無効／選択／エラー）。
3. **画面モック（横 1472×920）**: ホーム（通常・今日完了済み）／レッスン（read・drill）／キャンバス（ツールバー展開・採点結果パネル表示）／gesture（タイマー・見比べ）／批評（送信前・結果）／ギャラリー（Before/After 比較）／設定／レッスン完了モーダル／ステージ修了モーダル。
4. **縦向きのホームとキャンバス** 各1枚。
5. **ロゴとアプリアイコン**。
6. 可能なら **パスノードの3状態と門のマイクロインタラクション案**。

## 10. モック用サンプルデータ

**ユーザー状態**: ストリーク 23日、フリーズ 1、レベル 4（XP 1,240）、今日の目標 未達、現在: ステージ2 形と立体 U2-2 L3「箱を回す」、カウンター: 直線 1,234／楕円 512／円 300／箱 87/250／ジェスチャー 0／完成した絵 2。

**採点結果（直線ドリル）**: 点数 72（自己ベスト 78、▲-6）、直進度 70、到達 85、ブレ 64、助言「終点で速度が落ちて曲がっています。最後まで一定速度で」。

**批評結果（ステージ1.5 正面顔）**:
```json
{
  "good": ["十字線の左右対称がとれています", "目の高さが顔の中央に置けています"],
  "issues": [
    {"where": "右目", "what": "左目より一回り小さい", "fix": "目の幅を十字線の横線で測って揃える"},
    {"where": "口", "what": "鼻との距離が近い", "fix": "鼻から顎までの中点より少し上に置く"}
  ],
  "next_one": "次は目の幅だけを揃える練習を3回",
  "encourage": "1枚目でここまで形になれば十分です"
}
```

**レッスン例（read ステップ）**: 見出し「楕円の度合い」、本文「同じ円でも、見る角度で細くなります。この細さを『度合い』と呼びます。今日は度合いを揃える練習です。」、図解: 円→3段階の楕円の並び＋角度の矢印。

**パス例**: ステージ1 U1-3「円と楕円」L1〜L6（L1〜L4 完了、L5 今日、L6 ロック）、その先に「模写チェックポイント」「卒業課題」の門、次のステージ1.5 バナー「顔のミニ体験」（ロック）。
